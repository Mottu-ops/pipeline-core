name: Container on Kubernetes

on:
  workflow_call:
    inputs:
      username:
        required: true
        type: string

jobs:
  INIT:
    name: Initialization
    runs-on: ubuntu-latest
    steps:
      - run: echo "Veja dcoumentação da pipeline em"
      
  CI:
    name: Job1
    runs-on: ubuntu-latest
    needs:
      - INIT
    steps:
      - run: echo "🎉 oi ${{ inputs.username }}"
      - run: printenv | sort
      
  CD:
    name: Job2
    needs:
      - CI
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 oi ${{ inputs.username }}"
      - run: printenv | sort
