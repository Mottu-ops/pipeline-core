name: Deployment

on:
  workflow_call:
    inputs:
      username:
        required: true
        type: string
jobs:
  job1:
    name: Job2
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 oi ${{ inputs.username }}"
      - run: printenv | sort
      
  job2:
    name: Job2
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 oi ${{ inputs.username }}"
      - run: printenv | sort
